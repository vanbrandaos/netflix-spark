<configuration>
    <property>
        <name>dfs.replication</name>
        <value>1</value> 
    </property>

    <property>
        <name>dfs.namenode.name.dir</name>
        <value>/hadoop/dfs/name</value> <!-- Altere para o diretório onde deseja armazenar os dados do Namenode -->
    </property>

    <property>
        <name>dfs.datanode.name.dir</name>
        <value>/hadoop/dfs/data</value> <!-- Altere para o diretório onde deseja armazenar os dados do Namenode -->
    </property>    

    <property>
        <name>dfs.blocksize</name>
        <value>67108864</value> <!-- Tamanho do bloco em bytes (64MB) -->
    </property>
</configuration>


